#!/usr/bin/env ruby
# frozen_string_literal: true

$LOAD_PATH.unshift File.expand_path('../lib', __dir__)
$LOAD_PATH.unshift File.expand_path('../test', __dir__)

require 'test_helper'

Dir[File.expand_path('../test/**/*_test.rb', __dir__)].each do |file|
  require file
end
